export const albumsResMap = ({
  id,
  name,
  year,
  created_at,
  updated_at,
  song_id,
  title,
  performer,
}) => ({
  id,
  name,
  year,
  createdAt: created_at,
  updatedAt: updated_at,
  songId: song_id,
  title,
  performer,
});
